{
  "name": "snapserver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "spacy-install": "python3 -m pip install -U pip setuptools wheel && python3 -m pip install -U spacy && python3 -m spacy download en_core_web_sm"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "python-shell": "^3.0.1",
    "socket.io": "^4.4.1",
    "spacy": "^0.0.4"
  }
}
